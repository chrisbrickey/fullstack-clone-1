json.extract! user, :id, :username
#prob returns {id:  , username:  }

# add these after auth setup :name, :profile_img_url, :tagline

# need to construct the below after auth setup
# followersCount: 10
# followers: [2, ...],
# followingCount: 11
# following: [2, ...],
# photosCount: 15,
# photos: [2, ...]
